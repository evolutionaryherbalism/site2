{
  "name": "visual-regression-tests",
  "version": "1.0.0",
  "description": "Visual regression testing for monitored URLs",
  "scripts": {
    "test": "playwright test",
    "clean": "rm -rf tests/visual-regression.spec.js-snapshots test-results playwright-report",
    "test:remote": "gh workflow run visual-regression.yml",
    "update:remote": "gh workflow run update-baselines.yml"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "js-yaml": "^4.1.0"
  }
}
